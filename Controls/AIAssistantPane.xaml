<UserControl
    x:Class="SwellSSH.Controls.AIAssistantPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SwellSSH.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Width="350">

    <Grid Background="Transparent" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="1,0,0,0">
        <Grid.RowDefinitions>
            <!-- Header -->
            <RowDefinition Height="Auto" />
            <!-- Messages -->
            <RowDefinition Height="*" />
            <!-- Input -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="16" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE99A;" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                    <TextBlock Text="AI 助手" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center" />
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <Button x:Name="SettingsBtn" Padding="6" Background="Transparent" BorderThickness="0" Click="SettingsBtn_Click">
                        <FontIcon Glyph="&#xE713;" FontSize="14" />
                    </Button>
                    <Button x:Name="CloseBtn" Padding="6" Background="Transparent" BorderThickness="0" Click="CloseBtn_Click">
                        <FontIcon Glyph="&#xE711;" FontSize="12" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Messages List -->
        <ListView x:Name="MessagesList" Grid.Row="1" Padding="12,12,12,12" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="{Binding Alignment}">
                            <!-- Avatar / Role label -->
                            <TextBlock Text="{Binding RoleName}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,4" HorizontalAlignment="{Binding Alignment}" />
                            <!-- Bubble -->
                            <Border Background="{Binding BubbleBackground}" 
                                    CornerRadius="8" Padding="12,8">
                                <TextBlock Text="{Binding Text}" 
                                           TextWrapping="Wrap" 
                                           IsTextSelectionEnabled="True" 
                                           Foreground="{Binding BubbleForeground}" 
                                           FontFamily="{Binding BubbleFont}" />
                            </Border>
                            
                            <!-- AI Actions -->
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0" 
                                        Visibility="{Binding ActionsVisibility}" 
                                        HorizontalAlignment="Left">
                                <Button Content="复制" Padding="4,2" FontSize="11" Click="CopyMsg_Click" Tag="{Binding Text}" />
                                <Button Content="发送到终端" Padding="4,2" FontSize="11" Click="RunShell_Click" Tag="{Binding Text}" />
                                <Button Content="后台执行" Padding="4,2" FontSize="11" Click="RunBg_Click" Tag="{Binding Text}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Input Area -->
        <Border Grid.Row="2" Padding="12,12,12,4" BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}" BorderThickness="0,1,0,0" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" Spacing="16">
                    <CheckBox x:Name="AttachContextCheckbox" Content="附加上下文内容" FontSize="12" IsChecked="True" />
                </StackPanel>
                
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="InputBox" PlaceholderText="输入您的问题... (Shift+Enter 换行)" TextWrapping="Wrap" AcceptsReturn="True" MaxHeight="120" PreviewKeyDown="InputBox_PreviewKeyDown" />
                    <Button x:Name="SendBtn" Grid.Column="1" Margin="8,0,0,0" VerticalAlignment="Bottom" Style="{ThemeResource AccentButtonStyle}" Click="SendBtn_Click">
                        <FontIcon Glyph="&#xE122;" FontSize="14" />
                    </Button>
                </Grid>
                
                <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,8,0,0" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="" Visibility="Collapsed" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
